Got it! Reviews, X, Instagram, competitors — that's your friend's part, the signal source. Your part is everything after the signal arrives.
So your system is:
Signal arrives (from anywhere)  →  QA runs  →  Agentic fix  →  Reply back
Let's complete Phase 2 first — make the QA rock solid — then move to Phase 3 agentic fixer.
Here's exactly what we build one by one:
Phase 2 — QA (finish this first)
1. GitHub webhook    ← so QA auto-runs on every push
2. VS Code extension ← dev sees QA results live in sidebar
Phase 3 — Agentic fixer (after QA is solid)
3. AI reads report   ← finds exact file + line
4. AI writes fix     ← patches the code
5. Opens GitHub PR   ← auto PR with the fix
6. Re-runs QA        ← confirms fix worked
7. Replies back      ← to whoever sent the signal
